    //condição entre valor 1 se nao valor 2
    // condition ? value1 : value2

    let pao = true
    let queijo = false
    const niceBreakfast = pao && queijo ? 'cafe top' : 'cafe ruim'
    console.log(niceBreakfast)

    let age = 16
    const canDrive = age >= 18 ? "can drive" : "can't drive"
    console.log(canDrive)